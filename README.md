# 論証暗唱

司法試験の論証を、何も見ずに書き出す練習をするための1枚ものの Web アプリ。

- **中身は生成物。** 元は別リポジトリ（非公開）の論証 md で、`build_cards.py` →
  `publish_pages.py` で組み立てて置いている。**ここを手で直しても次の公開で消える。**
- 進捗はブラウザの localStorage に入る。端末をまたがない。
- 検索避け（`robots.txt` と `noindex`）を入れてある。

https://xnrmjghn6b-code.github.io/anshou/
